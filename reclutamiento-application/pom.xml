<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.soft</groupId>
        <artifactId>spring-base</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>reclutamiento-application</artifactId>
    <packaging>jar</packaging>
    <name>reclutamiento-application</name>
    <description>Application Layer - Application Services and Facades</description>

    <dependencies>
        <!-- ========================================== -->
        <!-- INTERNAL MODULES                           -->
        <!-- ========================================== -->
        <!-- Dependencia del módulo Application Abstractions -->
        <dependency>
            <groupId>com.soft</groupId>
            <artifactId>reclutamiento-application-abstractions</artifactId>
        </dependency>

        <!-- Dependencia del módulo Domain -->
        <dependency>
            <groupId>com.soft</groupId>
            <artifactId>reclutamiento-domain</artifactId>
        </dependency>

        <!-- Dependencia del módulo DTO -->
        <dependency>
            <groupId>com.soft</groupId>
            <artifactId>reclutamiento-dto</artifactId>
        </dependency>

        <!-- ========================================== -->
        <!-- CQRS / MEDIATOR                            -->
        <!-- ========================================== -->
        <!-- PipelinR para CQRS -->
        <dependency>
            <groupId>net.sizovs</groupId>
            <artifactId>pipelinr</artifactId>
        </dependency>

        <!-- ========================================== -->
        <!-- UTILITIES                                  -->
        <!-- ========================================== -->
        <!-- Apache Commons Lang3 -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- MapStruct (for optional mapping if needed) -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
        </dependency>

        <!-- ========================================== -->
        <!-- SPRING                                     -->
        <!-- ========================================== -->
        <!-- Spring Context for Component scanning -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>

        <!-- ========================================== -->
        <!-- TESTING                                    -->
        <!-- ========================================== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>${lombok-mapstruct-binding.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
